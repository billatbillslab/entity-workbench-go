# .release-removals — files this release removes from the published tree.
#
# One path per line; `#` comments allowed. Where something moved rather than went away,
# the entry says where to.

# Moved, not withdrawn: the rolling status log is now `docs/STATUS.md`, one level up.
# Same document, still published. `docs/status/` holds dated working notes and is not
# part of the published documentation set.
docs/status/STATUS.md
